services:
  - type: web
    name: boss-shop
    env: python
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python start.py
    envVars:
      - key: PORT
        value: "3000"
      - key: RENDER
        value: "true"
    autoDeploy: true